"""
========================================================================
文件名: __main__.py
所属模块: 命令行入口
========================================================================

让用户能 `python -m minisgl ...` 启动服务。
内容只有一行：调 launch_server() —— 真正的逻辑都在 server/launch.py。
========================================================================
"""

from .server import launch_server

assert __name__ == "__main__"

launch_server()
