"""
========================================================================
文件名: shell.py
所属模块: 项目根 - 交互式 shell 入口
========================================================================

让用户能 `python -m minisgl.shell ...` 启动一个本地的交互终端，
直接和模型对话——不开 HTTP server，单请求并发=1，CUDA graph 也只录单 bs。

适合本地调试、快速试模型、不想配 HTTP 客户端的场景。
========================================================================
"""

from .server import launch_server

if __name__ == "__main__":
    launch_server(run_shell=True)
